# Javascript Node CircleCI 2.0 configuration file

version: 2
jobs:
  build:
    docker:
      - image: circleci/node:11.7.0

    working_directory: ~/repo

    steps:
      - checkout
      - run: npm install
      - run: npm run test